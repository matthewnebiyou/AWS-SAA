name: s3-simple-sdk
description: A minimal AWS s3 bucket with python IaC
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
